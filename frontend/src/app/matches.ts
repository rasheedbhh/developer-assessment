//created an interface User, which means we can now have variables of type Match like we have variables of 
//type string and int, and user accepts these types of data 
export interface Match{
    id: number,
    name:string,
    image:string
}